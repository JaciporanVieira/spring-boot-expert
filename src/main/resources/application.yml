spring:
  application:
    name: Produtos API
  datasource:
    url: jdbc:h2:mem:produtos
    usarname: sa
    password: password
  jpa:
    database-plataform: org.hibernate.dialect.H@2Dialect
  h2:
    console:
      enable: true
      patch: /h2-console